<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="BorderButtonHot" Color="#9B9B9B"/>
    <SolidColorBrush x:Key="BorderButtonClick" Color="#9B9B9B"/>
    
    <Style TargetType="Window">
        <Setter Property="Foreground" Value="{DynamicResource ForeText}"/>
        <Setter Property="Background" Value="{DynamicResource BackText}"/>
    </Style>

    <ContextMenu x:Key="EditContextMenu" x:Shared="False">
        <MenuItem Command="ApplicationCommands.Undo"/>
        <Separator/>
        <MenuItem Command="ApplicationCommands.Cut"/>
        <MenuItem Command="ApplicationCommands.Copy"/>
        <MenuItem Command="ApplicationCommands.Paste"/>
        <MenuItem Command="EditingCommands.Delete" InputGestureText="Del"/>
        <Separator/>
        <MenuItem Command="ApplicationCommands.SelectAll"/>
    </ContextMenu>

    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{DynamicResource ForeText}"/>
        <Setter Property="Background" Value="{DynamicResource BackText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderTextBox}"/>
        <Setter Property="ContextMenu" Value="{StaticResource EditContextMenu}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderTextBoxFocused}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource ForeText}"/>
        <Setter Property="Background" Value="{DynamicResource BackButton}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderButton}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource SmallCornerRadius}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                            RecognizesAccessKey="True" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource BackButtonHot}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderButtonHot}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource BackButtonClick}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderButtonClick}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource ForeButtonDisabled}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderButtonDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Default_ComboBoxItemStyle" TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="4,1,1,1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border Background="{TemplateBinding Background}" 
                            BorderThickness="0" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource BackComboBoxItemHot}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource BackComboBoxItemSelected}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Default_ComboBoxEditStyle" TargetType="TextBox">
        <Setter Property="ContextMenu" Value="{StaticResource EditContextMenu}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <ScrollViewer x:Name="PART_ContentHost" Focusable="false" 
                        HorizontalScrollBarVisibility="Hidden" 
                        VerticalScrollBarVisibility="Hidden"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Foreground" Value="{DynamicResource ForeText}"/>
        <Setter Property="Background" Value="{DynamicResource BackComboBox}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderComboBox}"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource Default_ComboBoxItemStyle}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton Name="ToggleButton" Grid.Column="2" Focusable="false" ClickMode="Press"
                                SnapsToDevicePixels="True"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Foreground="{TemplateBinding Foreground}"
                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <Grid HorizontalAlignment="Right">
                                            <Path Name="Arrow" Stroke="{TemplateBinding Foreground}" 
                                                  Data="M 0,0 L 3,3 L 6,0" Margin="0,0,5,0"
                                                  VerticalAlignment="Center" StrokeThickness="0.6"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                            Content="{TemplateBinding SelectionBoxItem}"
                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                            Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" />

                        <TextBox Name="PART_EditableTextBox" Margin="3,3,0,3" Width="40"
                            HorizontalAlignment="Left" CaretBrush="{TemplateBinding Foreground}" Visibility="Hidden"
                            Foreground="{TemplateBinding Foreground}"
                            Background="{DynamicResource BackComboBoxEdit}"
                            Style="{StaticResource Default_ComboBoxEditStyle}"
                            IsReadOnly="{TemplateBinding IsReadOnly}"/>

                        <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                               SnapsToDevicePixels="True" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Grid Name="DropDown"
                                  MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Name="DropDownBorder" BorderThickness="1" Margin="0,0,0.5,0.5"
                                        Background="{DynamicResource BackComboBoxDropDown}" 
                                        BorderBrush="{DynamicResource BorderComboBoxDropDown}">
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="Background" Value="{DynamicResource BackText}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="Separator">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="6,4,6,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border BorderThickness="0,0,0,1" SnapsToDevicePixels="True"
                            BorderBrush="{DynamicResource BackMenuSeparator}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource BackContextMenu}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderContextMenu}"/>
        <Setter Property="Foreground" Value="{DynamicResource ForeContextMenu}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="{DynamicResource SmallCornerRadius}">
                        <ItemsPresenter Grid.IsSharedSizeScope="True"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource ForeContextMenu}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="2,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="templateRoot" SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource MenuItemCornerRadius}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="26" SharedSizeGroup="MenuIconColumn"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuHeaderColumn"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuShortcutColumn"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="IconContent" Margin="4,0,6,0" VerticalAlignment="Center"
                                              ContentSource="Icon"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            <Border x:Name="GlyphPanel" Width="22" Height="22" Margin="4,0,6,0" Visibility="Collapsed"
                                    Background="Transparent" BorderBrush="Transparent" BorderThickness="1"
                                    VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Grid>
                                    <Path x:Name="CheckMark" Data="M 0,5.3 L 2.6,8 L 7.6,2.3"
                                          Visibility="Hidden" Stretch="None" StrokeThickness="1.5"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          Stroke="{TemplateBinding Foreground}"/>
                                    <Path x:Name="Bullet" Data="M 0,3.5 A 3.5,3.5 0 1 1 7,3.5 A 3.5,3.5 0 1 1 0,3.5"
                                          Visibility="Hidden" Stretch="None" Fill="{TemplateBinding Foreground}"/>
                                </Grid>
                            </Border>
                            <ContentPresenter x:Name="HeaderContent" Grid.Column="1" ContentSource="Header"
                                              RecognizesAccessKey="True" Margin="5,0,30,0"
                                              VerticalAlignment="Center"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            <TextBlock x:Name="InputGestureText" Grid.Column="2" Margin="0,1,0,1"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"
                                       Text="{TemplateBinding InputGestureText}"
                                       Foreground="{TemplateBinding Foreground}"/>
                            <Path x:Name="RightArrow" Grid.Column="3" Data="M 0,0 L 4,3.5 L 0,7" Visibility="Collapsed"
                                  Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"
                                  SnapsToDevicePixels="True" Fill="{TemplateBinding Foreground}"/>
                            <Popup x:Name="PART_Popup" Placement="Right"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                    <Border BorderThickness="1" SnapsToDevicePixels="True"
                                            Background="{DynamicResource BackContextMenu}"
                                            BorderBrush="{DynamicResource BorderContextMenu}"
                                            CornerRadius="{DynamicResource SmallCornerRadius}">
                                    <ScrollViewer CanContentScroll="True"
                                                  Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer, TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                        <ItemsPresenter Grid.IsSharedSizeScope="True"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle"
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource BackMenuItemHot}"/>
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource BackMenuItemHot}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource ForeMenuItemDisabled}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="GlyphPanel" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="IconContent" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarLineButtonStyle" TargetType="RepeatButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{DynamicResource BackScrollBar}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BackScrollBar}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarPageButtonStyle" TargetType="RepeatButton">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border SnapsToDevicePixels="True" Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="{DynamicResource BackScrollBarThumb}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BackScrollBarThumb}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Rectangle RadiusX="3" RadiusY="3"
                        SnapsToDevicePixels="True"
                        Fill="{TemplateBinding Background}"
                        Stroke="{TemplateBinding BorderBrush}"
                        StrokeThickness="{TemplateBinding BorderThickness}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{DynamicResource BackScrollBar}"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid x:Name="Bg" SnapsToDevicePixels="True" Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="16"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="16"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                        </Grid.ColumnDefinitions>
                        <RepeatButton x:Name="PART_LineUpButton" Grid.Row="0" Grid.Column="0"
                                      Command="ScrollBar.LineUpCommand"
                                      Style="{StaticResource ScrollBarLineButtonStyle}">
                            <Path Data="M 0,4 L 4,0 L 8,4 Z"
                                  Stretch="None" Width="8" Height="4"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  SnapsToDevicePixels="True"
                                  Fill="{DynamicResource BackScrollBarGlyph}"/>
                        </RepeatButton>
                        <Track x:Name="PART_Track" Grid.Row="1" Grid.Column="0" Width="8"
                               IsDirectionReversed="True"
                               Orientation="{TemplateBinding Orientation}"
                               Minimum="{TemplateBinding Minimum}"
                               Maximum="{TemplateBinding Maximum}"
                               Value="{TemplateBinding Value}"
                               ViewportSize="{TemplateBinding ViewportSize}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="PART_PageUpButton" Command="ScrollBar.PageUpCommand" Style="{StaticResource ScrollBarPageButtonStyle}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton x:Name="PART_PageDownButton" Command="ScrollBar.PageDownCommand" Style="{StaticResource ScrollBarPageButtonStyle}"/>
                            </Track.IncreaseRepeatButton>
                        </Track>
                        <RepeatButton x:Name="PART_LineDownButton" Grid.Row="2" Grid.Column="0"
                                      Command="ScrollBar.LineDownCommand"
                                      Style="{StaticResource ScrollBarLineButtonStyle}">
                            <Path Data="M 0,0 L 4,4 L 8,0 Z"
                                  Stretch="None" Width="8" Height="4"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  SnapsToDevicePixels="True"
                                  Fill="{DynamicResource BackScrollBarGlyph}"/>
                        </RepeatButton>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="Bg" Property="Height" Value="16"/>
                            <Setter TargetName="Bg" Property="MinHeight" Value="16"/>
                            <Setter TargetName="PART_Track" Property="Grid.Row" Value="0"/>
                            <Setter TargetName="PART_Track" Property="Grid.Column" Value="1"/>
                            <Setter TargetName="PART_Track" Property="Orientation" Value="Horizontal"/>
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                            <Setter TargetName="PART_PageUpButton" Property="Command" Value="ScrollBar.PageLeftCommand"/>
                            <Setter TargetName="PART_PageDownButton" Property="Command" Value="ScrollBar.PageRightCommand"/>
                            <Setter TargetName="PART_LineDownButton" Property="Grid.Row" Value="0"/>
                            <Setter TargetName="PART_LineDownButton" Property="Grid.Column" Value="2"/>
                            <Setter TargetName="PART_LineUpButton" Property="Command" Value="ScrollBar.LineLeftCommand"/>
                            <Setter TargetName="PART_LineDownButton" Property="Command" Value="ScrollBar.LineRightCommand"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
